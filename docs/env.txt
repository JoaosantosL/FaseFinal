# Porta onde o servidor backend irá correr
PORT=3001
# Origem autorizada do frontend (ex: React em localhost ou produção)
FRONTEND_ORIGIN=http://localhost:3000
# URL da base de dados MongoDB Atlas
DB_URI=mongodb+srv://Nuno:Alunos2025@fichas.q0qrncc.mongodb.net/Ficha12?retryWrites=true&w=majority&appName=Fichas
# Segredo para assinar JWTs
JWT_SECRET=umaFraseSecretaMuitoForte
# Tempo de validade do token JWT (ex: 2d = 2 dias)
JWT_EXPIRES=2d
# Ativar logging detalhado com Winston (opcional)
LOG_LEVEL=info
# Ambiente da aplicação (development | production)
NODE_ENV=development
# Configurações de CORS
SAMESITE_POLICY=lax
# Configurações de rate limiting
# Logins permitidos por IP: 5 por hora
# Registos permitidos por IP: 10 por hora
RATE_LIMIT_LOGIN=5
RATE_LIMIT_REGISTER=10
RATE_LIMIT_PLAY=50
RATE_LIMIT_REACT=20
# Define se a sincronização de índices deve ser feita
SYNC_INDEXES=true